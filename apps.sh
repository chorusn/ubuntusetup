#!/bin/bash 

sudo apt update && sudo apt upgrade && sudo snap refresh && sudo snap install code --classic && sudo snap install codium --classic && sudo snap install snap-store firmware-updater obsidian localsend notion-desktop element-desktop varia discord telegram-desktop && sudo nala install flatpak gnome-software-plugin-flatpak && flatpak remote-add --if-not-exists flathub https://dl.flathub.org/repo/flathub.flatpakrepo && sudo flatpak install zapzap && sudo nala install gnome-shell-extension-manager gnome-weather gnome-tweaks build-essential ubuntu-restricted-extras timeshift 
